# A3 – Apache status check (simple)

Met dit experiment controleer ik of Apache2 draait op mijn server.
Ik gebruik hetzelfde principe als in de theorie: register en debug


## Uitvoeren
Ik ga naar de map van dit experiment en run:

```bash
ansible-playbook check_connectie.yaml
